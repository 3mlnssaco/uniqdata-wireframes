# AI 연구 설계 어시스턴트 스펙

## 개요

연구 설계 화면(Step 1~6)에서 AI와 대화하며 폼을 자동으로 채우는 기능.

---

## UI 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│  [사이드바]  │         [메인 폼]          │    [AI 채팅 패널]           │
│             │                            │                             │
│  ○ 홈       │  ┌─────────────────────┐   │  🤖 AI 연구 설계 어시스턴트  │
│  ○ 연구관리  │  │ Step 2: 설계 확인    │   │                             │
│  ○ Data Pool│  │                     │   │  [현재 연구 컨텍스트]        │
│  ○ 팀       │  │ 연령: [40] ~ [70]   │◀──│  당뇨 복약 순응도 연구       │
│  ○ 결제     │  │ 질환: ●당뇨 ○고혈압  │   │  40-70세 | 당뇨 | 6개 데이터│
│             │  │                     │   │                             │
│             │  │ [이전] [다음]       │   │  ────────────────────────   │
│             │  └─────────────────────┘   │                             │
│             │                            │  🤖 어떤 설정이 필요하신가요? │
│             │                            │                             │
│             │                            │  👤 40대 이상만 모집해줘     │
│             │                            │                             │
│             │                            │  🤖 참여 조건을 설정했습니다: │
│             │                            │  ┌────────────────────────┐ │
│             │                            │  │ ✓ 연령: 40세 이상       │ │
│             │                            │  │ [적용됨] [되돌리기]     │ │
│             │                            │  └────────────────────────┘ │
│             │                            │                             │
│             │                            │  [빠른 설정 칩들]           │
│             │                            │  ┌──────────────────────┐   │
│             │                            │  │  [입력]         [➤]  │   │
│             │                            │  └──────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## AI 처리 플로우

```
┌────────────────────────────────────────────────────────────────┐
│                                                                │
│  [사용자 입력]                                                  │
│      │                                                         │
│      ▼                                                         │
│  ┌─────────────────┐                                           │
│  │  프론트엔드 NLU │  ← 간단한 패턴 매칭 (오프라인)             │
│  │  (로컬 파싱)    │                                           │
│  └────────┬────────┘                                           │
│           │                                                    │
│           ▼                                                    │
│  ┌─────────────────┐     ┌─────────────────┐                   │
│  │  의도 분류      │────▶│  복잡한 요청    │                   │
│  │  (Intent)       │     │  → 백엔드 LLM   │                   │
│  └────────┬────────┘     └────────┬────────┘                   │
│           │ 단순 요청              │                           │
│           ▼                        ▼                           │
│  ┌─────────────────┐     ┌─────────────────┐                   │
│  │  로컬 액션 실행 │     │  LLM 응답 파싱  │                   │
│  │  (폼 업데이트)  │     │  → 액션 추출    │                   │
│  └────────┬────────┘     └────────┬────────┘                   │
│           │                        │                           │
│           └────────────┬───────────┘                           │
│                        ▼                                       │
│  ┌─────────────────────────────────────────┐                   │
│  │  DOM 업데이트 + 변경 이력 저장          │                   │
│  │  (undo 지원)                            │                   │
│  └─────────────────────────────────────────┘                   │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 지원 의도 (Intent) 목록

### Step 2: 설계 확인

| 의도 | 예시 발화 | 액션 |
|------|----------|------|
| `set_methodology` | "RCT로 바꿔줘", "코호트 연구로 해" | 방법론 선택 토글 |
| `set_age_range` | "40대 이상만", "30~50세" | 연령 입력 필드 업데이트 |
| `set_condition` | "당뇨 환자만", "고혈압 추가해" | 조건 칩 선택/해제 |
| `set_duration` | "60일로 해줘", "기간 늘려줘" | 기간 필드 업데이트 |
| `set_participants` | "100명 모집", "목표 인원 늘려" | 참여자 수 업데이트 |

### Step 3: 데이터 수집

| 의도 | 예시 발화 | 액션 |
|------|----------|------|
| `select_data` | "CGM 추가해", "심박수도 필요해" | 데이터 항목 선택 |
| `deselect_data` | "수면은 빼줘", "위치 데이터 삭제" | 데이터 항목 해제 |
| `set_ema_frequency` | "식단은 하루 2번만", "복약은 복용할 때마다" | EMA 빈도 설정 |
| `auto_recommend` | "AI 추천대로", "알아서 설정해줘" | 전체 추천 적용 |
| `reset` | "초기화해줘", "다 지워" | 전체 선택 해제 |

### Step 4: AI 도구

| 의도 | 예시 발화 | 액션 |
|------|----------|------|
| `select_tool` | "인과 분석 추가", "SHAP 써줘" | 도구 선택 |
| `explain_tool` | "이게 뭐야?", "왜 이거 추천해?" | 도구 설명 표시 |

### Step 5: 예산

| 의도 | 예시 발화 | 액션 |
|------|----------|------|
| `set_reward` | "하루 500원", "참여자당 3만원" | 보상 금액 설정 |
| `calculate_total` | "총 비용 얼마야?", "예산 계산해줘" | 예산 계산 트리거 |

---

## 백엔드 API

### 1. AI 의도 분석 (복잡한 요청)

```
POST /api/v2/ai/parse-intent
Authorization: Bearer {jwt}

Request:
{
  "message": "당뇨 환자 중에서 40대 이상, CGM 데이터 있는 사람만 모집하고 싶어",
  "context": {
    "step": 2,
    "current_state": {
      "methodology": "rct",
      "age_min": 30,
      "age_max": 60,
      "conditions": ["diabetes_t2"]
    }
  }
}

Response:
{
  "intents": [
    {
      "type": "set_condition",
      "params": { "id": "diabetes_t2", "action": "select" },
      "confidence": 0.95
    },
    {
      "type": "set_age_range",
      "params": { "min": 40, "max": null },
      "confidence": 0.92
    },
    {
      "type": "set_condition",
      "params": { "id": "cgm_user", "action": "select" },
      "confidence": 0.88
    }
  ],
  "response_text": "참여 조건을 설정했습니다:\n- 질환: 제2형 당뇨\n- 연령: 40세 이상\n- 필수 데이터: CGM",
  "actions_preview": [
    { "field": "연령", "from": "30-60세", "to": "40세 이상" },
    { "field": "조건", "from": "당뇨", "to": "당뇨, CGM 사용자" }
  ]
}
```

### 2. 추천 설정 조회

```
POST /api/v2/ai/recommend-settings
Authorization: Bearer {jwt}

Request:
{
  "project_id": "uuid",
  "step": 3,
  "research_question": "복약 순응도와 혈당 변동의 상관관계"
}

Response:
{
  "recommendations": {
    "data_collection": {
      "passive": ["cgm", "heart_rate", "sleep", "steps"],
      "active": ["medication", "diet", "mood"],
      "qualitative": ["meal_photo"]
    },
    "ema_settings": {
      "diet": "3_per_day",
      "mood": "2_per_day",
      "medication": "per_dose"
    }
  },
  "explanation": "복약-혈당 인과관계 분석을 위해 CGM과 복약 기록이 필수입니다. 식단 데이터는 혈당 변동 해석에 맥락을 제공합니다."
}
```

### 3. 변경 사항 적용 확인

```
POST /api/v2/ai/apply-changes
Authorization: Bearer {jwt}

Request:
{
  "project_id": "uuid",
  "changes": [
    { "type": "set_age_range", "params": { "min": 40, "max": 70 } },
    { "type": "select_data", "params": { "id": "cgm" } }
  ]
}

Response:
{
  "applied": true,
  "validation": {
    "zkp_circuits_valid": true,
    "estimated_matches": 1847,
    "warnings": []
  }
}
```

---

## 프론트엔드 구현

### 패턴 매칭 (로컬)

```javascript
const PATTERNS = {
  // 연령
  age_above: /(\d+)\s*(세|살)?\s*(이상|부터)/,
  age_range: /(\d+)\s*(세|살)?\s*[-~]\s*(\d+)\s*(세|살)?/,
  age_decade: /(\d+)0?\s*대/,

  // 질환
  diabetes: /(당뇨|diabetes)/i,
  hypertension: /(고혈압|hypertension)/i,

  // 데이터
  cgm: /(cgm|연속\s*혈당|혈당\s*모니터)/i,
  heart_rate: /(심박|heart\s*rate)/i,
  sleep: /(수면|sleep)/i,
  diet: /(식단|식사|meal|diet)/i,
  medication: /(복약|medication|약)/i,

  // 액션
  add: /(추가|넣어|포함|선택)/,
  remove: /(삭제|빼|제외|해제)/,
  recommend: /(추천|권장|알아서|자동)/,
  reset: /(초기화|리셋|clear|지워)/
};

function parseLocal(text) {
  const intents = [];

  // 연령 파싱
  let match = text.match(PATTERNS.age_above);
  if (match) {
    intents.push({
      type: 'set_age_range',
      params: { min: parseInt(match[1]), max: 100 }
    });
  }

  // 질환 파싱
  if (PATTERNS.diabetes.test(text)) {
    const action = PATTERNS.remove.test(text) ? 'deselect' : 'select';
    intents.push({
      type: 'set_condition',
      params: { id: 'diabetes_t2', action }
    });
  }

  // ... 기타 패턴

  return intents;
}
```

### DOM 업데이트

```javascript
function applyIntent(intent) {
  switch (intent.type) {
    case 'set_age_range':
      document.querySelector('[data-field="age_min"]').value = intent.params.min;
      document.querySelector('[data-field="age_max"]').value = intent.params.max || '';
      break;

    case 'set_condition':
      const chip = document.querySelector(`[data-id="${intent.params.id}"]`);
      chip?.classList.toggle('selected', intent.params.action === 'select');
      break;

    case 'select_data':
      const item = document.querySelector(`.data-item[data-id="${intent.params.id}"]`);
      item?.classList.add('selected');
      break;

    // ...
  }

  // 변경 이력 저장
  changeHistory.push({
    id: Date.now(),
    intent,
    timestamp: new Date()
  });
}
```

---

## 변경 이력 (Undo)

```javascript
const changeHistory = [];

function undo(actionId) {
  const index = changeHistory.findIndex(h => h.id === actionId);
  if (index === -1) return;

  const action = changeHistory[index];

  // 이전 상태로 복원
  switch (action.intent.type) {
    case 'set_age_range':
      document.querySelector('[data-field="age_min"]').value = action.previous.min;
      document.querySelector('[data-field="age_max"]').value = action.previous.max;
      break;
    // ...
  }

  changeHistory.splice(index, 1);
}
```

---

## 파일 구조

```
wireframes/research/
├── ai-chat-panel.css     # AI 채팅 패널 스타일
├── ai-chat-panel.js      # AI 채팅 로직
├── AI_CHAT_SPEC.md       # 이 문서
├── create.html           # Step 1: AI 설계
├── create-step2.html     # Step 2: 설계 확인 + AI 패널
├── create-step3.html     # Step 3: 데이터 수집 + AI 패널
├── create-step4.html     # Step 4: AI 도구 + AI 패널
├── create-step5.html     # Step 5: 예산 + AI 패널
└── create-step6.html     # Step 6: 에스크로 + AI 패널
```

---

### IRB 판단 (대화형 — uniqdata-ai 에이전트)

AI 채팅 패널에서 IRB 관련 판단도 대화형으로 진행된다.
단순 if-else가 아니라 LLM + RAG 기반으로 맥락적 판단.

| 의도 | 예시 발화 | 액션 |
|------|----------|------|
| `irb_check_required` | "IRB 심의 필요해?", "심의 대상인지 확인해줘" | IRB 판단 흐름 시작 (참고자료 2) |
| `irb_human_subject_check` | "인간대상연구야?", "설문조사인데 인간대상?" | 인간대상연구 해당 여부 판단 (시행규칙 제2조) |
| `irb_exemption_check` | "심의면제 가능해?", "면제 조건 확인해줘" | 심의면제 여부 판단 (시행규칙 제13조/제33조) |
| `irb_consent_exemption` | "동의 안 받아도 돼?", "서면동의 면제 가능?" | 서면동의면제 판단 (제16조3항) |
| `irb_required_docs` | "서류 뭐 필요해?", "제출서류 알려줘" | 유형별 필요 서류 체크리스트 표시 |
| `irb_generate_doc` | "연구계획서 만들어줘", "동의서 초안 생성해줘" | AI 서류 생성 트리거 |
| `irb_sample_size` | "대상자 몇 명 필요해?", "샘플 사이즈 계산해줘" | 대상자 수 산출근거 생성 |

### 논문 검색 (uniqdata-ai 에이전트)

| 의도 | 예시 발화 | 액션 |
|------|----------|------|
| `search_papers` | "관련 논문 찾아줘", "최신 연구 동향 알려줘" | PubMed/Scholar 검색 → 결과 제시 |
| `cite_methodology` | "비슷한 연구 방법론 있어?", "RCT 사례 보여줘" | 유사 방법론 논문 검색 |
| `reference_approved` | "비슷한 연구 승인 사례 있어?", "참고할 만한 IRB 승인 사례?" | RAG에서 승인 사례 검색 |

---

## 다음 단계

1. Step 4~6에 AI 패널 적용
2. 백엔드 `/ai/chat` 스트리밍 API 구현 (uniqdata-ai)
3. IRB 판단 대화 플로우 구현
4. RAG 파이프라인 구축 (법령/양식/논문 임베딩)
5. 논문 검색 API 연동 (PubMed, Semantic Scholar)
6. A/B 테스트: AI 패널 유무 비교

---

*Last Updated: 2026-02-12*
